## 问题分析

ChatBubble 继承自 QFrame，虽然设置了 `setFrameShape(QFrame.NoFrame)` 和 `setLineWidth(0)`，但可能 qdarktheme 的默认样式或其他样式仍然给父容器添加了边框。

## 解决方案

为 ChatBubble 添加样式表，明确设置无边框。

## 修改内容

在 ChatBubble.__init__ 中添加样式表设置。