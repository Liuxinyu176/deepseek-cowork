## 问题分析

`setScaledContents(True)` 会强制图片填满整个 QLabel，不保持宽高比，导致图片变形。

## 解决方案

1. 移除 `setScaledContents(True)`
2. 在加载图片时使用 `pixmap.scaled()` 方法，并指定 `Qt.KeepAspectRatio` 保持宽高比
3. 根据 `preview_image` 的当前大小进行缩放

## 修改内容

1. 移除 `setScaledContents(True)`
2. 修改图片加载逻辑，使用 `KeepAspectRatio` 缩放