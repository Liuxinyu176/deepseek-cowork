## 问题分析
1. **工作区路径显示区域** - `ws_container` 是局部变量，主题切换时无法更新样式
2. **内容预览区域** - 可能容器背景色设置不完整
3. **样式函数不完整** - `get_workspace_selector_style()` 缺少边框样式

## 解决方案
1. 将 `ws_container` 改为 `self.ws_container`，并在主题切换时更新
2. 完善 `get_workspace_selector_style()` 添加边框样式
3. 确保 `_on_theme_changed` 中更新工作区选择器样式