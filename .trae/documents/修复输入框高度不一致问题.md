## 问题分析

输入框高度问题：
1. 初始高度设置为 40px
2. 但 padding 上下加起来 20-24px，再加上字体 14-15px，40px 不够
3. 当用户输入时，`adjustHeight` 重新计算高度，导致高度变化

## 解决方案

1. 增加初始高度到 48px，确保能容纳 padding 和字体
2. 统一所有 padding 为 10px 16px
3. 确保 min_height 也设置为相同的值

## 修改内容

1. 修改 `AutoResizingInputEdit` 的初始高度为 48px
2. 统一 padding 设置